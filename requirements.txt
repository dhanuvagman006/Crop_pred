# Install everything (app + training):
#   pip install -r requirements.txt
#
# Install only the Streamlit app (smaller/faster):
#   pip install -r requirements.app.txt

-r requirements.app.txt
-r requirements.train.txt
